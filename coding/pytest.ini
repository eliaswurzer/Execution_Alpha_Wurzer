[pytest]
markers =
    unit: fast unit-level checks with synthetic data
    integration: synthetic cross-module integration checks
    realdata: opt-in checks that read local thesis TAQ parquet data
    slow: intentionally slower tests, excluded from quick local loops when needed
